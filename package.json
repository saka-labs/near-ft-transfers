{
  "name": "near-ft-transfers",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run src/index.ts --watch",
    "test:executor": "mkdir -p .tmp && TMPDIR=.tmp bun test tests/executor.test.ts",
    "bench:executor:sandbox": "mkdir -p .tmp && TMPDIR=.tmp bun run bench/executor-sandbox.bench.ts",
    "bench:executor:testnet": "mkdir -p .tmp && TMPDIR=.tmp bun run bench/executor-testnet.bench.ts",
    "bench:queue": "mkdir -p .tmp && TMPDIR=.tmp bun run bench/queue.bench.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@eclipseeer/near-api-ts": "^0.2.0",
    "@hono/swagger-ui": "^0.5.2",
    "@hono/zod-openapi": "^1.1.3",
    "@near-js/accounts": "^2.3.3",
    "@near-js/crypto": "^2.3.3",
    "@near-js/jsonrpc-types": "^1.4.0",
    "@near-js/providers": "^2.3.3",
    "@near-js/signers": "^2.3.3",
    "@near-js/tokens": "^2.3.3",
    "@near-js/transactions": "^2.3.3",
    "@near-js/utils": "^2.3.3",
    "bs58": "^6.0.0",
    "hono": "^4.9.9",
    "near-sandbox": "^0.2.0",
    "zod": "^4.1.11"
  }
}
